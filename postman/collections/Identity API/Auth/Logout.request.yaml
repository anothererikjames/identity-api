$kind: http-request
url: "{{baseUrl}}/auth/logout"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: afterResponse
    code: pm.test("Status is 204", () => pm.response.to.have.status(204));
order: 3000
